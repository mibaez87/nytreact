import React from 'react';
import { Card, CardTitle } from 'material-ui/Card';

const TitleCard = () => (
    <Card>
        <CardTitle title="New York Times Article Scrubber"
            subtitle="Search for and annotate articles of interest!" />
    </Card>
);

export default TitleCard;